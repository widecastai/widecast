# OS
.DS_Store
Thumbs.db

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
*.egg
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
build/
*.whl

# Node / TypeScript
node_modules/
dist/
.npm
*.tsbuildinfo

# Editors
.vscode/
.idea/
*.swp

# Local env / secrets
.env
.env.local
*.secret

# Legacy: build target used to be widecast/docs-html/ but is now widecast/
# itself (so the same URLs work both at /widecast/* during test and at /*
# in production). Old docs-html/ is stale — delete manually when ready.
docs-html/
