{
  "endpoint": {
    "method": "GET",
    "path": "/v1/platform_settings"
  },
  "title": "Load publish settings",
  "description": "Read-only, free. The saved per-platform publish preferences (privacy / page /\nsubreddit) — not access controls.\n",
  "fields": [],
  "poll_after_create": {
    "enabled": false
  },
  "use_cases": [
    {
      "id": "load_settings",
      "title": "⚙️ Load publish settings",
      "description": "No params — returns the saved settings object keyed by platform.",
      "request": {}
    }
  ],
  "_key": "platform-settings",
  "_api_base_url": "https://widecast.ai/app/dashboard"
}