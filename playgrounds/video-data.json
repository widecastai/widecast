{
  "endpoint": {
    "method": "POST",
    "path": "/v1/video_data"
  },
  "title": "Read structured scene data (data-first audit/edit entry point)",
  "description": "Synchronous, free. First step for data-first scene audit/edit — call\nthis BEFORE /v1/modify_scene or /v1/scene_inspector. URL-RESPONSE\nCONTRACT: the HTTP response is a small envelope; the full video_data\ntree is published to a temporary public JSON file on\n`origin.widecast.ai` and the envelope carries `data.url` + size +\n15-min TTL + a 3-step `instructions` block. The agent fetches the JSON\nfile with `curl <data.url>` and reads it locally — the full tree is\nNOT inlined. Bypasses MCP per-tool-call output caps that previously\ntruncated 50–300 KB payloads mid-JSON. Every call publishes a NEW\nunique file so URLs are never reused (matches realtime semantics).\nThe published JSON file content is the same as the legacy inline\nresponse: annotated segment dicts with stable-identity fields,\nRemotion spec metadata, and coordinate-space hints (720×1280 canvas\nvs 280×498 legacy editor preview). `voice_file` is the stable\nper-scene UID; `segment.id` is only current display/order.\n",
  "fields": [
    {
      "name": "video_id",
      "type": "text",
      "label": "video_id (topic_id — accepts widecast..., gubo..., or current ids)",
      "required": true,
      "placeholder": "widecastABCDEFGHIJKL"
    },
    {
      "name": "include_diagnostics",
      "type": "select",
      "label": "include_diagnostics (debug-only)",
      "required": false,
      "options": [
        {
          "value": "",
          "label": "false (default — actionable data only)"
        },
        {
          "value": "true",
          "label": "true (also return per-segment remotion_poster_* keys)"
        }
      ],
      "default": "",
      "help": "Default false. When true, each segment also carries `remotion_poster_*` diagnostic keys (and their copies inside `agent_meta.remotion_spec`) describing the server-side overlay poster used by /v1/scene_inspector fallback. For server/debug audits only."
    }
  ],
  "poll_after_create": {
    "enabled": false
  },
  "use_cases": [
    {
      "id": "read-video",
      "title": "📜 Audit a video — list every scene + Remotion spec status",
      "description": "Default response. Paste a topic_id (the id returned by /v1/create_video) to see scene_identity, every scene's voice_file (stable UID), current mediaUrl/mediaType, narrator config, and remotion_spec_url (cache-busted) + remotion_spec_state (ready/missing/disabled). Spec content is NOT inlined — fetch remotion_spec_url only when state is ready and you need object boxes.",
      "request": {
        "video_id": "widecastABCDEFGHIJKL"
      }
    },
    {
      "id": "with-diagnostics",
      "title": "🔬 Server/debug audit — include remotion_poster_*",
      "description": "Opt back into the diagnostic poster keys per-segment (file, url, version, state, exists, warnings). For server/debug audits only — agents don't act on these.",
      "request": {
        "video_id": "widecastABCDEFGHIJKL",
        "include_diagnostics": true
      }
    }
  ],
  "_key": "video-data",
  "_api_base_url": "https://widecast.ai/app/dashboard"
}