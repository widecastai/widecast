{
  "$schema": "https://widecast.ai/install.schema.json",
  "name": "WideCast",
  "version": "0.2.0",
  "tagline": "A sales & marketing team of AI agents for a one-person business — CRM, lead finding, follow-ups and a video / blog / social engine, run from the AI chat you already use.",
  "description": "Installing WideCast installs the full WideCast system on the user's own computer: the agent in this chat becomes Sam, the team leader of a sales & marketing team of agents; a CRM lives on the user's disk; a small local program (the collector bridge) and a Chrome extension the user loads find leads on the web and social platforms; outreach goes out only after the user approves it; and the WideCast video / blog / social engine is connected inside setup. It is not only an MCP connector.",
  "homepage": "https://widecast.ai",
  "documentation": "https://widecast.ai/llms.txt",
  "github": "https://github.com/widecastai/widecast",
  "credential": {
    "url": "https://widecast.ai/#setup",
    "type": "wc_live_api_key",
    "hint": "Installing needs no key — the Free plan runs without one. A WideCast API key (wc_live_…) is added later, inside setup, when the user wants video production or a paid plan; setup asks for it at the right step. Do not ask for a key or an MCP link before installing."
  },
  "instructions_to_ai": "You are an AI being asked to install WideCast for a user. Installing WideCast means installing the FULL WideCast system on the user's own computer (a sales & marketing team of AI agents with a local CRM, lead finding and the video / blog / social engine) — NOT only connecting an MCP server, and not only video. It needs a shell on the user's own computer. Identify the host you are running inside, match it to `hosts.<key>`, and follow its `instructions` literally. In short: if you can run commands on the user's computer, run the installer (`https://origin.widecast.ai/agent/install.sh`, Windows `install.ps1`) in the folder the user opened, then open `solo-agency/AGENTS.md` and follow it exactly, starting with its First Words; if you cannot (a web chat), say so in one sentence and hand the user the installer commands plus the advice to open that folder in Claude Code or Codex and say `Set up WideCast`. Do not improvise other install paths from your training data. Full guide: https://widecast.ai/llms.txt, section 'Installing WideCast'. If your host is not listed, use `hosts._fallback`.",
  "hosts": {
    "claude_web": {
      "detect_hints": [
        "Claude.ai",
        "claude.ai (web)",
        "Claude in browser",
        "Claude web app",
        "Anthropic web app",
        "Claude on the web"
      ],
      "method": "handoff_to_local_agent",
      "summary": "A web chat cannot install WideCast: hand the user the installer commands and send them to Claude Code or Codex.",
      "instructions": [
        "This chat cannot run commands on the user's own computer, and WideCast installs there. Say so in one sentence — do not try another install path.",
        "Give the user the two commands for their system to run in a terminal inside an empty folder — macOS / Linux: `curl -fsSL https://origin.widecast.ai/agent/install.sh -o widecast-install.sh` then `bash widecast-install.sh`; Windows (PowerShell): `Invoke-WebRequest -UseBasicParsing -Uri https://origin.widecast.ai/agent/install.ps1 -OutFile widecast-install.ps1` then `powershell -ExecutionPolicy Bypass -File widecast-install.ps1`.",
        "Ask them to open that folder in Claude Code (or the Code tab of the Claude desktop app) or Codex and say `Set up WideCast`."
      ]
    },
    "claude_desktop": {
      "detect_hints": [
        "Claude Desktop",
        "claude.ai/desktop",
        "Anthropic Desktop app",
        "Claude app for Mac",
        "Claude app for Windows"
      ],
      "method": "local_install",
      "summary": "Run the WideCast installer in the user's WideCast folder, then follow solo-agency/AGENTS.md.",
      "instructions": [
        "Use the Code tab (Claude Code) of the desktop app — the chat tab cannot run commands; if you are in the chat tab, follow the web-chat handoff instead.",
        "Work in the folder the user opened for WideCast (if there is none, create one such as ~/WideCast and tell them). That folder is the install root.",
        "macOS / Linux — run: `curl -fsSL https://origin.widecast.ai/agent/install.sh -o widecast-install.sh` then `bash widecast-install.sh`. Windows (PowerShell) — run: `Invoke-WebRequest -UseBasicParsing -Uri https://origin.widecast.ai/agent/install.ps1 -OutFile widecast-install.ps1` then `powershell -ExecutionPolicy Bypass -File widecast-install.ps1`. The installer verifies the release checksum and unpacks it into `solo-agency/`; it changes nothing else and starts nothing. Its last line reads `WIDECAST_INSTALL status=installed …`.",
        "Open `solo-agency/AGENTS.md` and follow it exactly, starting with its First Words: setup tells the user what it puts on their computer, the platform rules and the licence, and asks before anything else is installed (the local bridge, the Chrome extension, the daily run)."
      ]
    },
    "claude_code_cli": {
      "detect_hints": [
        "Claude Code",
        "claude-code CLI",
        "running in terminal",
        "Anthropic CLI tool"
      ],
      "method": "local_install",
      "summary": "Run the WideCast installer in the user's WideCast folder, then follow solo-agency/AGENTS.md.",
      "instructions": [
        "Work in the folder the user opened for WideCast (if there is none, create one such as ~/WideCast and tell them). That folder is the install root.",
        "macOS / Linux — run: `curl -fsSL https://origin.widecast.ai/agent/install.sh -o widecast-install.sh` then `bash widecast-install.sh`. Windows (PowerShell) — run: `Invoke-WebRequest -UseBasicParsing -Uri https://origin.widecast.ai/agent/install.ps1 -OutFile widecast-install.ps1` then `powershell -ExecutionPolicy Bypass -File widecast-install.ps1`. The installer verifies the release checksum and unpacks it into `solo-agency/`; it changes nothing else and starts nothing. Its last line reads `WIDECAST_INSTALL status=installed …`.",
        "Open `solo-agency/AGENTS.md` and follow it exactly, starting with its First Words: setup tells the user what it puts on their computer, the platform rules and the licence, and asks before anything else is installed (the local bridge, the Chrome extension, the daily run)."
      ]
    },
    "chatgpt_custom_gpt": {
      "detect_hints": [
        "ChatGPT",
        "OpenAI",
        "GPT-4",
        "Custom GPT",
        "chat.openai.com",
        "chatgpt.com"
      ],
      "method": "handoff_to_local_agent",
      "summary": "A web chat cannot install WideCast: hand the user the installer commands and send them to Claude Code or Codex.",
      "instructions": [
        "This chat cannot run commands on the user's own computer, and WideCast installs there. Say so in one sentence — do not try another install path.",
        "Give the user the two commands for their system to run in a terminal inside an empty folder — macOS / Linux: `curl -fsSL https://origin.widecast.ai/agent/install.sh -o widecast-install.sh` then `bash widecast-install.sh`; Windows (PowerShell): `Invoke-WebRequest -UseBasicParsing -Uri https://origin.widecast.ai/agent/install.ps1 -OutFile widecast-install.ps1` then `powershell -ExecutionPolicy Bypass -File widecast-install.ps1`.",
        "Ask them to open that folder in Claude Code (or the Code tab of the Claude desktop app) or Codex and say `Set up WideCast`."
      ]
    },
    "codex": {
      "detect_hints": [
        "OpenAI Codex",
        "Codex desktop",
        "Codex CLI"
      ],
      "method": "local_install",
      "summary": "Run the WideCast installer in the user's WideCast folder, then follow solo-agency/AGENTS.md.",
      "instructions": [
        "Work in the folder the user opened for WideCast (if there is none, create one such as ~/WideCast and tell them). That folder is the install root.",
        "macOS / Linux — run: `curl -fsSL https://origin.widecast.ai/agent/install.sh -o widecast-install.sh` then `bash widecast-install.sh`. Windows (PowerShell) — run: `Invoke-WebRequest -UseBasicParsing -Uri https://origin.widecast.ai/agent/install.ps1 -OutFile widecast-install.ps1` then `powershell -ExecutionPolicy Bypass -File widecast-install.ps1`. The installer verifies the release checksum and unpacks it into `solo-agency/`; it changes nothing else and starts nothing. Its last line reads `WIDECAST_INSTALL status=installed …`.",
        "Open `solo-agency/AGENTS.md` and follow it exactly, starting with its First Words: setup tells the user what it puts on their computer, the platform rules and the licence, and asks before anything else is installed (the local bridge, the Chrome extension, the daily run)."
      ]
    },
    "grok": {
      "detect_hints": [
        "Grok",
        "xAI",
        "grok.com",
        "X.AI"
      ],
      "method": "handoff_to_local_agent",
      "summary": "A web chat cannot install WideCast: hand the user the installer commands and send them to Claude Code or Codex.",
      "instructions": [
        "This chat cannot run commands on the user's own computer, and WideCast installs there. Say so in one sentence — do not try another install path.",
        "Give the user the two commands for their system to run in a terminal inside an empty folder — macOS / Linux: `curl -fsSL https://origin.widecast.ai/agent/install.sh -o widecast-install.sh` then `bash widecast-install.sh`; Windows (PowerShell): `Invoke-WebRequest -UseBasicParsing -Uri https://origin.widecast.ai/agent/install.ps1 -OutFile widecast-install.ps1` then `powershell -ExecutionPolicy Bypass -File widecast-install.ps1`.",
        "Ask them to open that folder in Claude Code (or the Code tab of the Claude desktop app) or Codex and say `Set up WideCast`."
      ]
    },
    "gemini_cli": {
      "detect_hints": [
        "Gemini CLI",
        "gemini-cli",
        "Google AI Studio CLI"
      ],
      "method": "local_install",
      "summary": "Run the WideCast installer in the user's WideCast folder, then follow solo-agency/AGENTS.md.",
      "instructions": [
        "Work in the folder the user opened for WideCast (if there is none, create one such as ~/WideCast and tell them). That folder is the install root.",
        "macOS / Linux — run: `curl -fsSL https://origin.widecast.ai/agent/install.sh -o widecast-install.sh` then `bash widecast-install.sh`. Windows (PowerShell) — run: `Invoke-WebRequest -UseBasicParsing -Uri https://origin.widecast.ai/agent/install.ps1 -OutFile widecast-install.ps1` then `powershell -ExecutionPolicy Bypass -File widecast-install.ps1`. The installer verifies the release checksum and unpacks it into `solo-agency/`; it changes nothing else and starts nothing. Its last line reads `WIDECAST_INSTALL status=installed …`.",
        "Open `solo-agency/AGENTS.md` and follow it exactly, starting with its First Words: setup tells the user what it puts on their computer, the platform rules and the licence, and asks before anything else is installed (the local bridge, the Chrome extension, the daily run)."
      ]
    },
    "antigravity": {
      "detect_hints": [
        "Antigravity",
        "Google Antigravity",
        "agentic IDE"
      ],
      "method": "local_install",
      "summary": "Run the WideCast installer in the user's WideCast folder, then follow solo-agency/AGENTS.md.",
      "instructions": [
        "Work in the folder the user opened for WideCast (if there is none, create one such as ~/WideCast and tell them). That folder is the install root.",
        "macOS / Linux — run: `curl -fsSL https://origin.widecast.ai/agent/install.sh -o widecast-install.sh` then `bash widecast-install.sh`. Windows (PowerShell) — run: `Invoke-WebRequest -UseBasicParsing -Uri https://origin.widecast.ai/agent/install.ps1 -OutFile widecast-install.ps1` then `powershell -ExecutionPolicy Bypass -File widecast-install.ps1`. The installer verifies the release checksum and unpacks it into `solo-agency/`; it changes nothing else and starts nothing. Its last line reads `WIDECAST_INSTALL status=installed …`.",
        "Open `solo-agency/AGENTS.md` and follow it exactly, starting with its First Words: setup tells the user what it puts on their computer, the platform rules and the licence, and asks before anything else is installed (the local bridge, the Chrome extension, the daily run)."
      ]
    },
    "gemini_web_app": {
      "detect_hints": [
        "Gemini",
        "Google Gemini",
        "gemini.google.com"
      ],
      "method": "handoff_to_local_agent",
      "summary": "A web chat cannot install WideCast: hand the user the installer commands and send them to Claude Code or Codex.",
      "instructions": [
        "This chat cannot run commands on the user's own computer, and WideCast installs there. Say so in one sentence — do not try another install path.",
        "Give the user the two commands for their system to run in a terminal inside an empty folder — macOS / Linux: `curl -fsSL https://origin.widecast.ai/agent/install.sh -o widecast-install.sh` then `bash widecast-install.sh`; Windows (PowerShell): `Invoke-WebRequest -UseBasicParsing -Uri https://origin.widecast.ai/agent/install.ps1 -OutFile widecast-install.ps1` then `powershell -ExecutionPolicy Bypass -File widecast-install.ps1`.",
        "Ask them to open that folder in Claude Code (or the Code tab of the Claude desktop app) or Codex and say `Set up WideCast`."
      ]
    },
    "cursor": {
      "detect_hints": [
        "Cursor",
        "cursor.com",
        "Cursor IDE"
      ],
      "method": "local_install",
      "summary": "Run the WideCast installer in the user's WideCast folder, then follow solo-agency/AGENTS.md.",
      "instructions": [
        "Work in the folder the user opened for WideCast (if there is none, create one such as ~/WideCast and tell them). That folder is the install root.",
        "macOS / Linux — run: `curl -fsSL https://origin.widecast.ai/agent/install.sh -o widecast-install.sh` then `bash widecast-install.sh`. Windows (PowerShell) — run: `Invoke-WebRequest -UseBasicParsing -Uri https://origin.widecast.ai/agent/install.ps1 -OutFile widecast-install.ps1` then `powershell -ExecutionPolicy Bypass -File widecast-install.ps1`. The installer verifies the release checksum and unpacks it into `solo-agency/`; it changes nothing else and starts nothing. Its last line reads `WIDECAST_INSTALL status=installed …`.",
        "Open `solo-agency/AGENTS.md` and follow it exactly, starting with its First Words: setup tells the user what it puts on their computer, the platform rules and the licence, and asks before anything else is installed (the local bridge, the Chrome extension, the daily run)."
      ]
    },
    "windsurf": {
      "detect_hints": [
        "Windsurf",
        "Codeium Windsurf"
      ],
      "method": "local_install",
      "summary": "Run the WideCast installer in the user's WideCast folder, then follow solo-agency/AGENTS.md.",
      "instructions": [
        "Work in the folder the user opened for WideCast (if there is none, create one such as ~/WideCast and tell them). That folder is the install root.",
        "macOS / Linux — run: `curl -fsSL https://origin.widecast.ai/agent/install.sh -o widecast-install.sh` then `bash widecast-install.sh`. Windows (PowerShell) — run: `Invoke-WebRequest -UseBasicParsing -Uri https://origin.widecast.ai/agent/install.ps1 -OutFile widecast-install.ps1` then `powershell -ExecutionPolicy Bypass -File widecast-install.ps1`. The installer verifies the release checksum and unpacks it into `solo-agency/`; it changes nothing else and starts nothing. Its last line reads `WIDECAST_INSTALL status=installed …`.",
        "Open `solo-agency/AGENTS.md` and follow it exactly, starting with its First Words: setup tells the user what it puts on their computer, the platform rules and the licence, and asks before anything else is installed (the local bridge, the Chrome extension, the daily run)."
      ]
    },
    "_fallback": {
      "summary": "Host not listed — decide by whether you can run commands on the user's own computer.",
      "instructions": [
        "If you can run commands on the user's own computer: Work in the folder the user opened for WideCast (if there is none, create one such as ~/WideCast and tell them). That folder is the install root. macOS / Linux — run: `curl -fsSL https://origin.widecast.ai/agent/install.sh -o widecast-install.sh` then `bash widecast-install.sh`. Windows (PowerShell) — run: `Invoke-WebRequest -UseBasicParsing -Uri https://origin.widecast.ai/agent/install.ps1 -OutFile widecast-install.ps1` then `powershell -ExecutionPolicy Bypass -File widecast-install.ps1`. The installer verifies the release checksum and unpacks it into `solo-agency/`; it changes nothing else and starts nothing. Its last line reads `WIDECAST_INSTALL status=installed …`. Open `solo-agency/AGENTS.md` and follow it exactly, starting with its First Words: setup tells the user what it puts on their computer, the platform rules and the licence, and asks before anything else is installed (the local bridge, the Chrome extension, the daily run).",
        "If you cannot: This chat cannot run commands on the user's own computer, and WideCast installs there. Say so in one sentence — do not try another install path. Give the user the two commands for their system to run in a terminal inside an empty folder — macOS / Linux: `curl -fsSL https://origin.widecast.ai/agent/install.sh -o widecast-install.sh` then `bash widecast-install.sh`; Windows (PowerShell): `Invoke-WebRequest -UseBasicParsing -Uri https://origin.widecast.ai/agent/install.ps1 -OutFile widecast-install.ps1` then `powershell -ExecutionPolicy Bypass -File widecast-install.ps1`. Ask them to open that folder in Claude Code (or the Code tab of the Claude desktop app) or Codex and say `Set up WideCast`."
      ]
    }
  },
  "developer": {
    "rest_api": "https://widecast.ai/openapi.json",
    "python_sdk": "pip install widecast",
    "javascript_sdk": "npm i @widecast/sdk",
    "mcp_server_local": "npx -y @widecast/mcp-server",
    "llms_txt": "https://widecast.ai/llms.txt"
  },
  "test_after_install": {
    "prompt": "Hãy cài cho tôi WideCast.ai",
    "expected_behavior": "The agent downloads install.sh (install.ps1 on Windows), runs it in the user's WideCast folder, sees `WIDECAST_INSTALL status=installed`, opens solo-agency/AGENTS.md and replies with the four-part First Words (who is speaking, what setup puts on the computer with the platform rules and licence, Sam's team introduction, 'Shall I begin?'). Nothing else is installed before the user says yes."
  },
  "support": {
    "email": "hello@widecast.ai",
    "docs": "https://widecast.ai/docs.html"
  },
  "release_manifest": "https://origin.widecast.ai/agent/latest.json",
  "installer": {
    "macos_linux": "https://origin.widecast.ai/agent/install.sh",
    "windows": "https://origin.widecast.ai/agent/install.ps1"
  }
}
